diff --git a/apps/api/app/Http/Controllers/Api/EmployeeController.php b/apps/api/app/Http/Controllers/Api/EmployeeController.php
index 9ff0b79..ffb87be 100644
--- a/apps/api/app/Http/Controllers/Api/EmployeeController.php
+++ b/apps/api/app/Http/Controllers/Api/EmployeeController.php
@@ -65,7 +65,7 @@ class EmployeeController extends Controller
             'employee_code' => ['required', 'string', 'max:40', Rule::unique('employees', 'employee_no')->ignore($id)],
             'name' => 'required|string|max:120',
             'email' => 'nullable|email|max:255',
-            'phone' => 'nullable|string|max:40',
+            'phone' => ['nullable', 'string', 'max:40', 'regex:/^[0-9+\-\s()]{7,20}$/'],
             'position' => 'nullable|string|max:80',
             'department' => 'nullable|string|max:80',
             'hire_date' => $id === null ? 'nullable|date' : 'sometimes|date',
@@ -74,6 +74,8 @@ class EmployeeController extends Controller
             'basic_salary' => 'nullable|numeric|min:0',
             'hourly_rate' => 'nullable|numeric|min:0',
             'leave_balance' => 'nullable|numeric|min:0',
+        ], [
+            'phone.regex' => 'Phone must contain only digits, spaces, +, -, and parentheses (7-20 characters).',
         ]);
 
         $record = [
